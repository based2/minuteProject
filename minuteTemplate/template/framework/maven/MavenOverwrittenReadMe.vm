Put the resources configuration (for example: spring configuration) that are overwritting the generated configuration for the standard distribution package.

This is usefull, if you want to choose another connection pool/jndi configuration than the one proposed by the generation.
